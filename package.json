{
  "name": "mylist",
  "version": "1.0.0",
  "description": "simple list dodo or shopping",
  "main": "main.js",
  "scripts": {
    "build": "browserify main.js -o bundle.js",
    "watch": "watchify main.js -o bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vi",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^17.0.0",
    "html-validator": "^6.0.1",
    "watchify": "^4.0.0"
  }
}
